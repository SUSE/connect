Installed Products:
------------------------------------------
<% product_statuses.each do |product_status| %>
  <%= product_status.installed_product.summary %> <%= product_status.installed_product.arch %>

  <%= product_status.registration_status %>
  <% if product_status.related_activation -%>
  Subscription:
    <% activation = product_status.related_activation %>
      Regcode: <%= activation.regcode %>
      Starts at: <%= Time.parse(activation.starts_at) %>
      Expires at: <%= Time.parse(activation.expires_at) %>
      Status: <%= activation.status %>
      Type: <%= activation.type %>
  <% end %>
------------------------------------------
<% end %>
